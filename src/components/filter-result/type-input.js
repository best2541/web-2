import React from "react"
import { Input } from "reactstrap"

const TypeInput = () => {
  const onChange = () => {}

  return <Input type="text" placeholder="Enter" name="text" id="text" onChange={onChange} />
}

export default TypeInput
